{
  "name": "@crossword/core",
  "version": "0.1.0",
  "description": "Core types, interfaces and domain models for the Crossword Platform",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "tsc -w",
    "test": "vitest run --dir src --passWithNoTests",
    "lint": "eslint .",
    "format": "prettier --write \"src/**/*.ts\""
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
